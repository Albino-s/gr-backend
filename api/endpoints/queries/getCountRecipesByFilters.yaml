x-name: getCountRecipesByFilters
summary: getCountRecipesByFilters
tags:
  - Queries
parameters:
  - $fs-ref: ../../globals/auth-params.yaml
  - name: tagIds
    in: query
    description: tagIds
    type: string
    required: true
  - name: countTags
    in: query
    description: countTags
    type: string
    required: true
  - name: withIngredientIds
    in: query
    description: withIngredientIds
    type: string
    required: true
  - name: countWithIngredients
    in: query
    description: countWithIngredients
    type: string
    required: true
  - name: search
    in: query
    description: search
    type: string
  - name: time
    in: query
    description: time
    type: string
    required: true
  - name: withoutIngredientIds
    in: query
    description: withoutIngredientIds
    type: string
    required: true
responses:
  403:
    $ref: '#/responses/Forbidden'
  200:
    description: The array of the category IDs
  400:
    description: Invalid user ID supplied
    schema:
      $ref: '#/definitions/InvalidParameterError'
  500:
    $ref: '#/responses/Unexpected'